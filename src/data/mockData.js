/**
 * Nuri System Mock Constants & Baseline Reference Arrays
 * (All fake/seeded patient databases have been removed)
 */

// Baseline 10-point flatline/neutral sparkline arrays for initial unread telemetry
export const initialSparklines = {
  heartRate: [],
  spO2: [],
  temperature: []
};

// Initial empty OPD database (starts clean with 0 fake patients)
export const doctorPatientDatabase = [];
